{
  "name": "@orca-web/gcode",
  "version": "0.0.0",
  "private": true,
  "description": "Streaming G-code parser that compiles a toolpath into the compact per-layer preview format (M5)",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  }
}

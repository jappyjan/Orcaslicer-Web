{
  "name": "@orca-web/extractors",
  "version": "0.0.0",
  "private": true,
  "description": "Build-time extractors: PrintConfig.cpp option schema and the resolved profile catalog",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@orca-web/shared": "*"
  }
}
